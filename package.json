{
    "name": "thinkt-web-lite",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "THINKT_API_TOKEN=$(~/wethinkt/go-thinkt/bin/thinkt server token 2>/dev/null) vite",
        "build": "tsc && vite build",
        "build:fast": "tsc && vite build",
        "preview": "vite preview",
        "lint": "eslint src",
        "lint:fix": "eslint src --fix",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@wethinkt/ts-thinkt": "0.1.16",
        "eslint": "^10.0.2"
    },
    "author": "Neomantra Corp",
    "license": "MIT",
    "devDependencies": {
        "typescript": "^5.0.0",
        "vite": "^5.0.0"
    }
}